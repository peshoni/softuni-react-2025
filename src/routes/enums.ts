export enum PathSegments {
    LOGIN = 'login',
    REGISTER = 'register',
    USERS = 'users',
    VEHICLES = 'vehicles',
    DETAILS = 'details',
    REPAIR_REQUESTS = 'repair-requests',
    ID = ':id'
}