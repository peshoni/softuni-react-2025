export enum PathSegments {
    LANDING_PAGE = 'landingPage',
    LOGIN = 'login',
    REGISTER = 'register',
    CUSTOMERS = 'customers',
    VEHICLES = 'vehicles',

    DETAILS = 'details',
    REPAIR_REQUESTS = 'repair-requests',
    ID = ':id',
    CREATE = 'create',
    VEHICLE_ID = ':vehicleId'
} 