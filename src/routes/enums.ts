export enum PathSegments {
    LANDING_PAGE = 'landing-page',
    LOGIN = 'login',
    REGISTER = 'register',
    USERS = 'users',
    VEHICLES = 'vehicles',
    DETAILS = 'details',
    REPAIR_REQUESTS = 'repair-requests',
    ID = ':id'
}